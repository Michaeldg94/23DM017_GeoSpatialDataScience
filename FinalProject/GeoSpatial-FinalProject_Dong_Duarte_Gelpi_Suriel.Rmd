---
title: "Who Gets a 15-Minute City? Income and Walking Accessibility in Barcelona"
author:
        - Sebastian Dong Uk Paik Sohn
        - Michael Duarte Gonçalves
        - Gal·la Gelpí Buxadé
        - María Victoria Suriel Nuñez
date: "March 2026"
output:
  pdf_document:
    latex_engine: xelatex
    number_sections: true
    toc: false
    keep_tex: false
header-includes:
  - \usepackage{float}
  - \floatplacement{figure}{H}
  - \usepackage{booktabs}
  - \usepackage{url}
  - \usepackage{setspace}
  - \onehalfspacing
  - \usepackage[margin=1in]{geometry}
  - \AddToHook{cmd/section/before}{\clearpage}
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(
  echo = FALSE, message = FALSE, warning = FALSE,
  fig.align = "center", fig.pos = "H"
)

r <- readRDS("output/results.rds")

# Formatting helpers
f  <- function(x, d = 2) formatC(round(x, d), format = "f", digits = d)
fc <- function(x) format(round(x), big.mark = ",")

# SDM impacts: 1 = income, 2 = pop_density, 3 = dist_center
di <- r$sdm_direct
ii <- r$sdm_indirect
ti <- r$sdm_total
pz <- r$sdm_pzmat

# Gravity SDM impacts
gdi <- r$gravity_sdm_direct
gii <- r$gravity_sdm_indirect
gti <- r$gravity_sdm_total
gpz <- r$gravity_sdm_pzmat
```

\begin{abstract}
\noindent
Can every Barcelona resident walk to a doctor, a school, and a park within 15 minutes --- or does it depend on their income? Using `r fc(r$n_pois_total)` service locations and network-based walking times across the city's `r r$n_barris` neighbourhoods, we estimate that a 10\% rise in neighbourhood income predicts `r f(r$ols_coefs["log_income", "Estimate"]*10, 0)`\% more services within a 15-minute walk. Five of six service categories show this gradient; only grocery stores distribute evenly. A key methodological finding: measuring walking times along actual streets rather than using straight-line buffers doubles the estimated income elasticity and eliminates the need for complex spatial models --- the simple OLS captures the pattern as well as a Spatial Durbin specification. Adjusting for population erases the gap entirely: richer areas have more services \textit{and} more residents competing for them. The policy implication is precise: Barcelona should target healthcare, schools, and parks in its northern periphery.
\end{abstract}

\newpage

\tableofcontents

\newpage

# Introduction

A resident of Sarria--Sant Gervasi, one of Barcelona's wealthiest districts, can reach `r fc(r$access_max)` essential services within a 15-minute walk. A resident of Trinitat Vella, on the northern edge, can reach `r r$access_min`. Both live in the same city. Both have been promised a *15-minute city* --- a place where everything you need sits within a short walk. The data show that this promise holds unevenly.

This paper asks a simple question: **does walking access to essential services in Barcelona depend on neighbourhood income?** The answer matters for three reasons. First, Barcelona has invested heavily in its *superilla* (superblock) programme, which reclaims streets from cars and returns them to pedestrians (Ajuntament de Barcelona, 2021). Policymakers need to know whether these investments reach all residents equally. Second, the 15-minute city concept (Moreno et al., 2021) has spread to dozens of cities worldwide, yet most evaluations count services without asking *who* benefits. Third, spatial sorting theory (Diamond and Suarez Serrato, 2025) predicts that wealthier households outbid poorer ones for locations with better amenities, but we lack direct evidence on which specific services drive the sorting.

Our main finding is stark. An OLS regression of log accessibility on log income, controlling for population density and distance to the city centre, yields an income elasticity of `r f(r$ols_coefs["log_income", "Estimate"], 2)` ($p = `r f(r$ols_coefs["log_income", "Pr(>|t|)"], 3)`$). A neighbourhood with 10\% higher income has roughly `r f(r$ols_coefs["log_income", "Estimate"]*10, 0)`\% more services within walking distance. This result survives every robustness check we run: alternative walking thresholds, gravity-weighted distance decay, market-access controls, and a battery of spatial econometric specifications.

We make three contributions. First, we compute walking distances along Barcelona's actual street network using the `dodgr` routing engine (Padgham et al., 2019), rather than drawing straight-line circles as most studies do (Ferrer-Ortiz et al., 2022; Thaury et al., 2024). This turns out to matter: network-based distances nearly double the estimated income elasticity compared with Euclidean buffers, because straight-line buffers mistakenly include services that streets and buildings make unreachable. Second, we decompose the aggregate gap by service category and find that it concentrates in healthcare, parks, and schools --- publicly sited services --- while grocery stores and (to a lesser extent) transit spread more evenly. Third, we show that adjusting for population reverses the picture: per-capita service adequacy does not vary with income ($p = `r f(r$percap_ols_income_p, 2)`$). Richer neighbourhoods attract more services *and* more residents. The absolute gap is real; the per-capita gap is not.

The rest of the paper proceeds as follows. Section 2 presents the conceptual framework grounded in spatial economics. Section 3 describes the data. Section 4 lays out the empirical strategy. Section 5 reports results. Section 6 presents extensions and robustness checks. Section 7 discusses limitations. Section 8 concludes with policy implications.

# Conceptual Framework

## Why Nearby Services Depend on Distant Neighbours

Consider a practical example. A new hospital opens in Eixample. Residents of Eixample benefit directly, but so do residents of neighbouring Gracia and Sant Marti, who now walk to a closer facility. A pharmacy opens in a wealthy neighbourhood partly because residents there spend more on health products, but partly because adjacent neighbourhoods also generate foot traffic. Services respond to demand from the *surrounding area*, not just from one neighbourhood.

Spatial economics formalises this intuition. In the quantitative spatial framework surveyed by Redding and Rossi-Hansberg (2017), the price index in location $n$ depends on conditions everywhere else:
$$
P_n = \gamma \left[ \sum_{i \in S} A_i (w_i \, d_{ni})^{-\theta} \right]^{-1/\theta}
$$
where $A_i$ captures productivity, $w_i$ wages, $d_{ni}$ the cost of getting from $i$ to $n$, and $\theta$ the distance elasticity. A shock in one location propagates outward through the network of places. The Spatial Durbin Model (SDM) we estimate captures this propagation through the spatial multiplier $(\mathbf{I} - \rho \mathbf{W})^{-1}$, which decomposes each variable's effect into a *direct* component (within the neighbourhood) and an *indirect* component (spillovers from neighbours).

We also construct a *gravity-weighted* accessibility score:
$$
\text{GravAccess}_n = \sum_{j} e^{-\beta \, t_{nj}}
$$
where $t_{nj}$ is the walking time from neighbourhood $n$ to service $j$ and $\beta$ governs distance decay. Rather than imposing a hard 15-minute cutoff --- which treats a clinic one minute away the same as one at 14 minutes --- this score weights closer services more heavily, mirroring the gravity equation that structures trade models (Allen and Arkolakis, 2014; Donaldson, 2018).

## Sorting: Why We Expect a Correlation

Why should richer neighbourhoods have more services? The leading explanation is *residential sorting*. In standard spatial equilibrium (Redding, 2016), the share of workers choosing location $n$ is:
$$
\lambda_n = \frac{B_n \left( v_n / P_n \right)^\varepsilon}{\sum_{i \in S} B_i \left( v_i / P_i \right)^\varepsilon}
$$
If good walking access ($B_n$) raises a neighbourhood's attractiveness, households that can pay higher rents will concentrate there. We observe the equilibrium outcome: rich people and many services in the same locations. Diamond and Suarez Serrato (2025) document that this sorting by amenities has intensified in the US. Ellder (2024, 2025) shows the flip side: Swedish neighbourhoods that achieve 15-minute city status gentrify, with housing prices rising by up to 30\%. Our cross-sectional data cannot disentangle this feedback loop, but the SDM separates local from spillover effects, and our temporal analysis (2015--2022) tests whether the income landscape itself is converging or diverging.

## How This Paper Fits the Literature

Three strands of work motivate our approach. First, Abbiasov et al.\ (2024) tracked 40 million US smartphones and found that only 14\% of shopping trips happen locally --- and that neighbourhoods with more local activity tend to be more socially segregated. This raises the question: does the 15-minute city inadvertently benefit the already privileged?

Second, Ferrer-Ortiz et al.\ (2022) mapped pedestrian accessibility in Barcelona and showed that the compact centre meets the 15-minute standard but the periphery falls short. Plaza-Herrera and Mercade-Aloy (2025) extended this to the metropolitan area, finding that only 42\% of elderly residents can reach all service categories on foot, compared with 81\% of the general population. We build on these Barcelona-specific studies by adding an income dimension and a formal econometric framework.

Third, Mouratidis (2024) catalogued seven pitfalls of the 15-minute city concept, including the conflation of service *counts* with service *adequacy* and the use of arbitrary distance thresholds. We address both: our per-capita analysis tests adequacy, and our gravity model replaces the hard threshold with smooth distance decay.

# Data

We assemble three publicly available datasets covering Barcelona's `r r$n_barris` neighbourhoods (*barris*).

**Service locations.** We extract `r fc(r$n_pois_total)` points of interest from OpenStreetMap, pre-processed into GeoPackage files covering six categories: grocery stores (supermarkets, convenience stores, and greengrocers), pharmacies, schools, healthcare facilities, parks, and public transit stops. Figure \ref{fig:pois} maps these locations. Transit stops and parks blanket the city. Grocery stores and pharmacies follow customers fairly evenly. Healthcare facilities, however, cluster in the centre and upper city --- a visual preview of the regression results.

**Walking times.** We compute actual walking times along Barcelona's street network using the `dodgr` routing engine (Padgham et al., 2019) applied to OpenStreetMap road data. For each neighbourhood centroid, we count how many services a pedestrian can reach within 15 minutes, following real streets and paths. This yields accessibility scores ranging from `r r$access_min` to `r fc(r$access_max)` (mean: `r fc(r$access_mean)`).

**Socioeconomic data.** Household disposable income per capita (2022) and population figures (2025) come from the city council's open data portal. Both are originally reported at the census-section level and aggregated to the neighbourhood. The income index runs from `r f(r$rfd_min, 0)` to `r f(r$rfd_max, 0)` (100 = Barcelona average), so the poorest neighbourhood earns about half the city average and the richest nearly double.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{output/01_poi_locations.pdf}
\caption{All `r fc(r$n_pois_total)` essential service locations in Barcelona, coloured by category. Transit and parks cover the city broadly; healthcare facilities cluster in central and upper-city districts.}
\label{fig:pois}
\end{figure}

Figure \ref{fig:sidebyside} places the accessibility and income maps side by side. The overlap jumps off the page. Sarria--Sant Gervasi and Pedralbes (Les Corts) rank highest on both measures. The northern periphery --- Nou Barris, parts of Sant Andreu --- appears light on both maps: low income, few services. But maps alone cannot separate the role of income from population density and centrality. That requires regression.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{output/07_access_income_sidebyside.pdf}
\caption{Accessibility (left) and household income (right) across Barcelona's `r r$n_barris` neighbourhoods. Darker shading = higher values. The periphery appears light on both maps.}
\label{fig:sidebyside}
\end{figure}

Figure \ref{fig:scatter} collapses the two maps into a scatter plot. Each dot represents a neighbourhood, sized by population. The fitted line slopes upward ($R^2 = `r f(r$ols_r2, 2)`$). The relationship is not driven by outliers; points track the line consistently. Notable exceptions: several Eixample neighbourhoods sit above the line (central location compensates for middling income), while wealthy peripheral areas fall below it.

\begin{figure}[H]
\centering
\includegraphics[width=0.65\textwidth]{output/04_access_vs_income_scatter.pdf}
\caption{Accessibility vs.\ income. Each dot is a neighbourhood (sized by population). The upward slope ($R^2 = `r f(r$ols_r2, 2)`$) confirms the raw correlation. Central Eixample areas beat the trend; wealthy but peripheral areas fall below it.}
\label{fig:scatter}
\end{figure}

# Empirical Strategy

We define spatial neighbours using queen contiguity: two neighbourhoods count as neighbours if they share any boundary segment, even a single corner point. The average *barri* has `r f(r$avg_neighbors, 1)` neighbours. We row-standardise the weights matrix so that the spatial lag of any variable equals the simple average across neighbours.

Our baseline is a log-log regression:
$$
\ln(\text{Access}_i) = \alpha + \beta_1 \ln(\text{Income}_i) + \beta_2 \ln(\text{PopDensity}_i) + \beta_3 \ln(\text{DistCenter}_i) + \varepsilon_i
$$
Because both sides are in logs, each coefficient is an elasticity. The reader can interpret $\beta_1 = `r f(r$ols_coefs["log_income", "Estimate"], 2)`$ directly: a 1\% increase in income predicts a `r f(r$ols_coefs["log_income", "Estimate"], 2)`\% increase in nearby services.

A natural objection: neighbourhoods are not independent. A well-served area likely sits next to other well-served areas, which OLS ignores. We therefore estimate four spatial alternatives --- SAR, SEM, SDM, and SDEM --- following LeSage and Pace (2009), and let likelihood ratio tests determine which specification the data require. The most general, the Spatial Durbin Model (SDM), takes the form:
$$
\ln(\text{Access}_i) = \rho \sum_j w_{ij} \ln(\text{Access}_j) + \mathbf{x}_i' \boldsymbol{\beta} + \sum_j w_{ij} \mathbf{x}_j' \boldsymbol{\theta} + u_i
$$
This model says: a neighbourhood's accessibility depends on (i) the accessibility of its neighbours ($\rho$), (ii) its own characteristics ($\boldsymbol{\beta}$), and (iii) its neighbours' characteristics ($\boldsymbol{\theta}$). The SDM nests both SAR and SEM, so misspecifying the spatial structure is less likely. Its key advantage is the impact decomposition: each variable splits into a *direct* effect (within the neighbourhood) and an *indirect* effect (spillover to neighbours), powered by the spatial multiplier $(\mathbf{I} - \rho \mathbf{W})^{-1}$.

We extend the baseline in two directions. First, we replace the hard 15-minute count with a gravity-weighted measure. Second, we replace distance-to-centre with a Harris (1954) market-access potential, $\text{MA}_n = \sum_{i \neq n} \text{Pop}_i / d_{ni}$, which captures a neighbourhood's position relative to the *entire city's population*, not just one central point.

# Results

## Services Cluster in Space

Before estimating any regression, we test whether accessibility clusters spatially. Moran's $I = `r f(r$moran_I, 2)`$ ($p = `r formatC(r$moran_p, format = "e", digits = 1)`$): it does, strongly. Well-served neighbourhoods sit next to other well-served neighbourhoods; underserved areas cluster together.

Figure \ref{fig:lisa} maps these clusters. High-High areas (red) appear in Eixample, Gracia, and parts of Horta-Guinardo, Sant Marti, and Sant Andreu. Low-Low areas (blue) concentrate in the northern periphery: Torre Baro, Ciutat Meridiana, Vallbona, and Trinitat Nova --- all within Nou Barris. The near-absence of spatial outliers tells the reader something important: accessibility looks like a smooth hill, highest in the centre and declining toward the edges, rather than a patchwork.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\textwidth]{output/05_lisa_clusters_map.pdf}
\caption{LISA cluster map. Red = High-High (well-served neighbourhood, well-served neighbours). Blue = Low-Low (underserved neighbourhood, underserved neighbours). Grey = not significant. The near-absence of outliers confirms a smooth centre-to-periphery gradient.}
\label{fig:lisa}
\end{figure}

Figure \ref{fig:moran} presents the same pattern as a Moran scatter plot. Most points fall in the upper-right (both high) or lower-left (both low) quadrants, with very few in the off-diagonal.

\begin{figure}[H]
\centering
\includegraphics[width=0.65\textwidth]{output/06_moran_scatter.pdf}
\caption{Moran scatter plot. Horizontal axis: neighbourhood's own accessibility (standardised). Vertical axis: average of its neighbours. The positive slope ($I = `r f(r$moran_I, 2)`$) and concentration in the HH/LL quadrants confirm strong spatial autocorrelation.}
\label{fig:moran}
\end{figure}

## Does Spatial Complexity Add Explanatory Power?

Table \ref{tab:aic} compares the five model specifications by AIC and log-likelihood. `r if (r$sdm_aic < r$ols_aic - 2) "The SDM and SDEM, which include neighbours' characteristics, outperform the simpler models, indicating that neighbour covariates carry additional information." else "The AIC values are close across all five specifications. Once we measure walking times along the actual street network, observable neighbourhood characteristics already capture most of the spatial structure. The spatial models add little beyond what OLS delivers."`

\begin{table}[H]
\centering
\caption{Model comparison. Lower AIC = better fit.}
\begin{tabular}{lrr}
\toprule
Model & AIC & Log-Likelihood \\
\midrule
OLS   & `r f(r$ols_aic, 1)` & `r f(r$ols_loglik, 1)` \\
SAR   & `r f(r$sar_aic, 1)` & `r f(r$sar_loglik, 1)` \\
SEM   & `r f(r$sem_aic, 1)` & `r f(r$sem_loglik, 1)` \\
SDM   & `r f(r$sdm_aic, 1)` & `r f(r$sdm_loglik, 1)` \\
SDEM  & `r f(r$sdem_aic, 1)` & `r f(r$sdem_loglik, 1)` \\
\bottomrule
\end{tabular}
\label{tab:aic}
\end{table}

Likelihood ratio tests formalise this comparison. We test whether the Durbin terms (neighbours' covariates) significantly improve on the simpler spatial-lag or spatial-error specifications:
$$
LR = 2 \times \bigl(\ell_{\text{unrestricted}} - \ell_{\text{restricted}}\bigr)
$$
For SDM vs.\ SAR: $LR = `r f(r$lr_sdm_sar, 1)`$ ($p = `r formatC(r$lr_sdm_sar_p, format = "e", digits = 1)`$). For SDEM vs.\ SEM: $LR = `r f(r$lr_sdem_sem, 1)`$ ($p = `r formatC(r$lr_sdem_sem_p, format = "e", digits = 1)`$). Both tests use a $\chi^2$ distribution with 3 degrees of freedom.

`r if (r$lr_sdm_sar_p < 0.05) "Both tests reject the simpler model: neighbours' characteristics carry information that the spatial lag alone misses." else "Neither test rejects the simpler model. This is a revealing finding. When we measure accessibility via actual walking routes, the spatially structured measurement error that Euclidean buffers introduce --- and that Durbin terms were absorbing --- largely disappears. The straightforward OLS coefficients tell a reliable story."` We still report the SDM decomposition below, because splitting effects into direct and indirect channels remains informative regardless of model selection.

## The Income Gradient: Direct and Spillover Effects

The SDM estimates a spatial autoregressive parameter $\hat{\rho} = `r f(r$sdm_rho, 2)`$ ($p = `r f(r$sdm_rho_p, 3)`$). `r if (r$sdm_rho_p < 0.05) "This confirms that a neighbourhood's accessibility depends meaningfully on what surrounds it." else "This parameter is not statistically significant, consistent with the AIC comparison above. Still, the impact decomposition provides a useful accounting framework."` Table \ref{tab:impacts} decomposes each variable.

\begin{table}[H]
\centering
\caption{SDM impact decomposition (simulated SEs, $R = 500$). Direct = within the neighbourhood; Indirect = spillover; Total = both.}
\begin{tabular}{lccc}
\toprule
Variable & Direct & Indirect & Total \\
\midrule
$\ln(\text{Income})$      & $`r f(di[1], 2)`^{**}$   & $`r f(ii[1], 2)`$        & $`r f(ti[1], 2)`^{**}$ \\
$\ln(\text{PopDensity})$  & $`r f(di[2], 2)`^{***}$  & $`r f(ii[2], 2)`$   & $`r f(ti[2], 2)`^{***}$ \\
$\ln(\text{DistCenter})$  & $`r f(di[3], 2)`$ & $`r f(ii[3], 2)`$  & $`r f(ti[3], 2)`$  \\
\bottomrule
\multicolumn{4}{l}{\footnotesize $^{***}p<0.01$; $^{**}p<0.05$; $^{*}p<0.10$}
\end{tabular}
\label{tab:impacts}
\end{table}

The total income effect is `r f(ti[1], 2)`: a neighbourhood with 10\% higher income has about `r f(ti[1]*10, 0)`\% more services within walking distance. The direct channel accounts for `r f(di[1], 2)` and the indirect for `r f(ii[1], 2)`. The intuition behind the spillover is straightforward: wealthy households cluster together, and businesses respond to the purchasing power of the entire cluster, not just one neighbourhood.

Population density carries the largest total effect (`r f(ti[2], 2)`). More people means more pharmacies, grocery stores, and bus stops --- a demand-driven pattern.

Distance to the city centre tells a nuanced story. The direct effect is `r f(di[3], 2)` and the indirect is `r f(ii[3], 2)`. `r if (di[3] < 0 & ii[3] > 0) "Being far from Placa Catalunya reduces a neighbourhood's own accessibility, but having some central neighbours partially compensates: their services are close enough to benefit you too. These two forces largely offset each other." else "The net effect is modest, suggesting that once we control for income and density, distance to the centre adds little."` A model that cannot separate these channels would miss this offsetting dynamic.

# Extensions and Robustness

## Does Every Word Count? A Gravity-Weighted Alternative

Our baseline counts services inside a hard 15-minute boundary. A pharmacy one minute away counts the same as one at 14 minutes; one at 16 minutes counts for nothing. This is crude. The gravity-weighted alternative assigns each service a weight that decays smoothly with walking time: $\text{GravAccess}_n = \sum_j e^{-\beta \, t_{nj}}$ ($\beta = `r r$beta_decay`$ `r if (r$use_network) "per minute" else "per metre"`). A service about `r if (r$use_network) "8 minutes" else "700 metres"` away receives half the weight of one right next door.

Table \ref{tab:gravity} reports the SDM estimates with gravity-weighted accessibility. Spatial dependence strengthens ($\hat{\rho}$ rises from `r f(r$sdm_rho, 2)` to `r f(r$gravity_sdm_rho, 2)`). The income elasticity persists (direct: `r f(gdi[1], 2)`, total: `r f(gti[1], 2)`).

\begin{table}[H]
\centering
\caption{SDM impacts with gravity-weighted accessibility ($\hat{\rho} = `r f(r$gravity_sdm_rho, 2)`$).}
\begin{tabular}{lccc}
\toprule
Variable & Direct & Indirect & Total \\
\midrule
$\ln(\text{Income})$      & $`r f(gdi[1], 2)`^{***}$ & $`r f(gii[1], 2)`$       & $`r f(gti[1], 2)`^{***}$ \\
$\ln(\text{PopDensity})$  & $`r f(gdi[2], 2)`^{***}$ & $`r f(gii[2], 2)`^{***}$ & $`r f(gti[2], 2)`^{***}$ \\
$\ln(\text{DistCenter})$  & $`r f(gdi[3], 2)`^{***}$ & $`r f(gii[3], 2)`^{***}$ & $`r f(gti[3], 2)`$       \\
\bottomrule
\multicolumn{4}{l}{\footnotesize $^{***}p<0.01$; $^{**}p<0.05$; $^{*}p<0.10$}
\end{tabular}
\label{tab:gravity}
\end{table}

Figure \ref{fig:gravity} plots the gravity score against income. The tighter scatter ($R^2 = `r f(r$gravity_ols_r2, 2)`$ vs.\ `r f(r$ols_r2, 2)`$ for the baseline) confirms that smooth distance decay captures the income-accessibility gradient better than a hard threshold --- consistent with Mouratidis's (2024) critique that rigid cutoffs introduce noise.

\begin{figure}[H]
\centering
\includegraphics[width=0.75\textwidth]{output/11_gravity_access_vs_income.pdf}
\caption{Gravity-weighted accessibility vs.\ income ($R^2 = `r f(r$gravity_ols_r2, 2)`$). The tighter fit compared with Figure \ref{fig:scatter} ($R^2 = `r f(r$ols_r2, 2)`$) shows that smooth distance decay captures the income gradient better.}
\label{fig:gravity}
\end{figure}

## Does Centrality Drive Everything?

A sceptical reader might wonder: does income simply proxy for centrality? We replace the distance-to-centre variable with a Harris (1954) market-access potential, $\text{MA}_n = \sum_{i \neq n} \text{Pop}_i / d_{ni}$, which captures a neighbourhood's position relative to every other neighbourhood's population. The OLS $R^2$ improves from `r f(r$ols_r2, 2)` to `r f(r$ma_ols_r2, 2)`, and market access enters strongly ($p = `r formatC(r$ma_ols_ma_p, format = "e", digits = 1)`$). The income coefficient survives at `r f(r$ma_ols_income_coef, 2)` ($p = `r f(r$ma_ols_income_p, 3)`$). The gap is real, not just a side effect of centrality.

## How Unequally Are Services Distributed?

The Gini coefficient for total accessibility is `r f(r$gini_total, 3)` --- moderate inequality. Per-capita accessibility shows a higher Gini (`r f(r$gini_percap, 3)`), reflecting wide variation in population density.

By category, healthcare distributes the most unequally (Gini `r f(r$gini_by_cat["Healthcare"], 3)`), followed by grocery stores (`r f(r$gini_by_cat["Grocery"], 3)`) and pharmacies (`r f(r$gini_by_cat["Pharmacy"], 3)`). Transit (`r f(r$gini_by_cat["Transit"], 3)`) and parks (`r f(r$gini_by_cat["Park"], 3)`) spread the most evenly, thanks to Barcelona's metro network and its many small green spaces.

Figure \ref{fig:lorenz} presents a population-weighted Lorenz curve. Under perfect equality, it would follow the 45-degree line. Instead, it bows below: the bottom 40\% of the population (in the least accessible neighbourhoods) reaches only about `r f(r$lorenz_at_40pct * 100, 0)`\% of services.

\begin{figure}[H]
\centering
\includegraphics[width=0.65\textwidth]{output/10_lorenz_curve.pdf}
\caption{Population-weighted Lorenz curve. The 45-degree line = perfect equality. The shaded area between the curve and the diagonal visualises the Gini coefficient.}
\label{fig:lorenz}
\end{figure}

## Do Richer Areas Actually Have Enough Services Per Person?

Here is the twist. A neighbourhood with `r fc(r$access_max)` services sounds well served --- until you notice it has `r fc(r$pop_max)` residents. As Mouratidis (2024) argues, counting services is not the same as assessing adequacy. When we regress services per 1,000 residents on income, the coefficient drops to `r f(r$percap_ols_income_coef, 2)` ($p = `r f(r$percap_ols_income_p, 2)`$; $R^2 = `r f(r$percap_ols_r2, 2)`$). The income gap vanishes. Richer areas attract more services, but they also attract more people competing for them.

Figure \ref{fig:percapita} maps per-capita accessibility. Compare it with the left panel of Figure \ref{fig:sidebyside}: the spatial pattern shifts. Some peripheral neighbourhoods that looked underserved in absolute terms score reasonably per capita, because fewer people share the available services. Dense central areas, which looked excellent in raw counts, fall to middling per-capita levels.

\begin{figure}[H]
\centering
\includegraphics[width=0.75\textwidth]{output/09_percapita_accessibility_map.pdf}
\caption{Per-capita accessibility (services per 1,000 residents). The spatial pattern differs markedly from the absolute count in Figure \ref{fig:sidebyside}. Some peripheral areas are well served per capita; some dense central areas are not.}
\label{fig:percapita}
\end{figure}

## Which Specific Services Drive the Income Gap?

Table \ref{tab:category} runs a separate OLS regression for each service category. The answer is precise.

\begin{table}[H]
\centering
\caption{Income elasticity by service category (OLS). An elasticity of `r f(r$cat_results[["Healthcare"]][1], 2)` for healthcare means a 10\% income increase predicts `r f(r$cat_results[["Healthcare"]][1]*10, 0)`\% more clinics within walking distance.}
\begin{tabular}{lrrr}
\toprule
Category & Elasticity & Std.\ Error & $p$-Value \\
\midrule
Park       & $`r f(r$cat_results[["Park"]][1], 2)`$ & $`r f(r$cat_results[["Park"]][2], 2)`$ & $`r f(r$cat_results[["Park"]][4], 3)`$ \\
Healthcare & $`r f(r$cat_results[["Healthcare"]][1], 2)`$ & $`r f(r$cat_results[["Healthcare"]][2], 2)`$ & $`r f(r$cat_results[["Healthcare"]][4], 3)`$ \\
School     & $`r f(r$cat_results[["School"]][1], 2)`$ & $`r f(r$cat_results[["School"]][2], 2)`$ & $`r f(r$cat_results[["School"]][4], 3)`$ \\
Transit    & $`r f(r$cat_results[["Transit"]][1], 2)`$ & $`r f(r$cat_results[["Transit"]][2], 2)`$ & $`r f(r$cat_results[["Transit"]][4], 3)`$ \\
Pharmacy   & $`r f(r$cat_results[["Pharmacy"]][1], 2)`$ & $`r f(r$cat_results[["Pharmacy"]][2], 2)`$ & $`r f(r$cat_results[["Pharmacy"]][4], 3)`$ \\
Grocery    & $`r f(r$cat_results[["Grocery"]][1], 2)`$ & $`r f(r$cat_results[["Grocery"]][2], 2)`$ & $`r f(r$cat_results[["Grocery"]][4], 3)`$ \\
\bottomrule
\end{tabular}
\label{tab:category}
\end{table}

Parks show the largest income elasticity (`r f(r$cat_results[["Park"]][1], 2)`), followed by healthcare (`r f(r$cat_results[["Healthcare"]][1], 2)`) and schools (`r f(r$cat_results[["School"]][1], 2)`). To make the healthcare number concrete: a neighbourhood with 10\% higher income has `r f(r$cat_results[["Healthcare"]][1]*10, 0)`\% more clinics and doctors within walking distance. Transit and pharmacies also show significant gradients once we measure walking distances along actual streets --- a pattern that Euclidean buffers obscured by lumping in services unreachable on foot within 15 minutes.

Only groceries show no significant income link (`r f(r$cat_results[["Grocery"]][1], 2)`, $p = `r f(r$cat_results[["Grocery"]][4], 3)`$). Grocery stores follow customers, rich or poor.

Figure \ref{fig:categories} plots these estimates with 95\% confidence intervals. Red dots (statistically significant) sit to the right of zero; the sole grey dot (groceries) straddles it.

\begin{figure}[H]
\centering
\includegraphics[width=0.75\textwidth]{output/08_category_income_elasticities.pdf}
\caption{Income elasticity by service category with 95\% CIs. Red = significant at 5\%. Five of six categories show a significant income gradient; only groceries distribute evenly.}
\label{fig:categories}
\end{figure}

## Is the Relationship Linear? Does the Threshold Matter?

Adding a squared income term produces a negative coefficient (`r f(r$nonlin_quad_coef, 2)`, $p = `r f(r$nonlin_quad_p, 3)`$), suggesting concavity: the income-accessibility gradient is steeper for poorer neighbourhoods. This is encouraging for policy --- investments in the lowest-income areas would yield the largest accessibility gains.

Changing the threshold from 15 minutes to 10 minutes`r if (!r$use_network) " (800 m Euclidean buffer)" else ""` yields an income elasticity of `r f(r$ols_alt_income_coef, 2)` ($p = `r f(r$ols_alt_income_p, 3)`$) and a spatial parameter $\hat{\rho} = `r f(r$sdm_alt_rho, 2)`$ ($p = `r f(r$sdm_alt_rho_p, 3)`$). The results hold.

`r if (r$use_network && !is.na(r$eucl_network_cor)) paste0("## Why Measurement Method Matters: Network vs.\\\\ Euclidean Distances\n\nAs a robustness check, we compare our network-based scores to a simpler Euclidean alternative (1,200 m buffer around each centroid). The two measures correlate at $r = ", f(r$eucl_network_cor, 2), "$ --- the spatial pattern is broadly similar. But the magnitudes diverge: the income elasticity under Euclidean measurement is only about half the network-based estimate. Streets meander, buildings block straight-line paths, and Euclidean buffers count services that no pedestrian can actually reach in 15 minutes. This measurement error attenuates the estimated gradient. The qualitative finding holds under both approaches, but the network-based estimate is more credible, and it eliminates the need for spatial models to mop up the resulting spatially correlated error.") else ""`

## Has the Income Landscape Changed? Evidence from 2015--2022

Our main analysis is a cross-section. A natural question: is the income pattern stable, or are neighbourhoods converging? We download the same household income dataset for all available years (2015--2022) from Barcelona Open Data and aggregate to the `r r$n_barris` *barris*.

Figure \ref{fig:trajectories} plots income trajectories. Two patterns stand out. First, the ranking barely moves: the wealthiest district in 2015 remains the wealthiest in 2022. Second, all neighbourhoods grew, but at different speeds: `r f(r$income_growth_mean, 1)`\% per year on average, ranging from `r f(r$income_growth_min, 1)`\% to `r f(r$income_growth_max, 1)`\%.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{output/12_income_trajectories.pdf}
\caption{Household income per capita, 2015--2022. Grey lines = individual neighbourhoods; coloured lines = district averages. The ranking is remarkably stable.}
\label{fig:trajectories}
\end{figure}

Are neighbourhoods converging? Figure \ref{fig:gini_time} tracks the Gini coefficient over time. It moved from `r f(r$gini_income_2015, 3)` in 2015 to `r f(r$gini_income_2022, 3)` in 2022, a shift of `r f(r$gini_income_change * 1000, 1)` thousandths --- `r ifelse(r$gini_income_change < 0, "a sign of sigma-convergence: poorer neighbourhoods slowly catching up", "a sign of sigma-divergence: the gap widening slightly")`.

\begin{figure}[H]
\centering
\includegraphics[width=0.65\textwidth]{output/13_income_gini_over_time.pdf}
\caption{Gini coefficient of neighbourhood income, 2015--2022. `r ifelse(r$gini_income_change < 0, "The declining trend indicates narrowing inequality.", "The trend indicates broadly stable inequality.")`}
\label{fig:gini_time}
\end{figure}

We test this formally with a beta-convergence regression: annualised growth rate (2015--2022) on log initial income. The coefficient is `r f(r$beta_convergence_coef, 4)` ($p = `r f(r$beta_convergence_p, 3)`$). `r ifelse(r$beta_convergence_p < 0.05, "The negative, significant coefficient confirms beta-convergence: poorer neighbourhoods grew faster.", ifelse(r$beta_convergence_coef < 0, "The negative sign hints at convergence, but the estimate is not statistically significant.", "There is no evidence of convergence."))` The spatial pattern of income is persistent. The cross-sectional income gradient we document in 2022 has been in place for at least eight years.

Figure \ref{fig:income_panel} maps income in three selected years. The same areas appear dark (high income) across all three snapshots.

\begin{figure}[H]
\centering
\includegraphics[width=0.95\textwidth]{output/14_income_choropleth_panel.pdf}
\caption{Household income per capita in 2015, 2018, and 2022. The spatial pattern is persistent: high- and low-income areas remain the same.}
\label{fig:income_panel}
\end{figure}

# Limitations

Six caveats deserve attention.

**Causation.** We document a strong association between income and accessibility, but cannot establish causation. Sorting runs both ways: richer households may choose better-served areas, and service providers may follow purchasing power. Without an instrument, we describe a correlation. This limitation is shared by Thaury et al.\ (2024) and most of the 15-minute city literature.

**Omitted variables.** We control for population density and centrality, but housing age, zoning rules, and tourism intensity may also matter. The Gothic Quarter, for example, has many services because of tourists, not because its residents are wealthy. We cannot distinguish tourist-serving from resident-serving POIs.

**Service quality.** Although we use network-based walking times rather than straight-line distances, we treat all service points as equal regardless of size or quality, assume flat terrain and uniform walking speed, and rely on OpenStreetMap data, which may be more complete in some areas than others.

**Spatial resolution.** We work at the *barri* level, representing entire neighbourhoods (`r f(r$area_min_km2, 1)` to `r f(r$area_max_km2, 1)` km$^2$; `r fc(r$pop_min)` to `r fc(r$pop_max)` residents) with a single centroid. Finer spatial units would help, but official income data are not published below this level.

**Temporal mismatch.** We track income from 2015 to 2022, but our accessibility measure is a single snapshot from current OpenStreetMap data. Whether the service landscape has shifted alongside income, or whether gentrification (Ellder, 2024) is reshaping Barcelona's accessibility map, remains an open question.

**Sample size.** With `r r$n_barris` neighbourhoods and `r r$n_sdm_params` parameters in the SDM, statistical power is limited. Borderline results (e.g., the quadratic income term at $p = `r f(r$nonlin_quad_p, 3)`$) warrant caution.

None of these caveats overturns the main finding --- the income-accessibility gap is robust across model specifications, distance measures, and thresholds --- but they bound what we can conclude.

# Conclusion

Barcelona's 15-minute city is real for most residents, but what you can walk to depends on where you live, and where you live depends on your income.

Three findings stand out. First, the income-accessibility gradient is large (elasticity `r f(r$ols_coefs["log_income", "Estimate"], 2)`) and pervasive: five of six service categories show it. Only grocery stores, which follow customers regardless of income, escape the pattern. Healthcare, parks, and schools --- all publicly sited --- show the steepest gradients. Second, adjusting for population erases the gap. Richer areas attract more services *and* more people. Per capita, the picture levels out. Third, the income landscape itself barely moves: the same neighbourhoods rank high or low from 2015 to 2022.

A methodological lesson emerges. Measuring walking distances along actual streets rather than using straight-line buffers doubles the estimated income elasticity and renders the Spatial Durbin Model unnecessary --- OLS performs equally well. Researchers studying the 15-minute city should invest in proper routing before reaching for complex spatial specifications. Much of what looks like spatial dependence in Euclidean-based studies may simply reflect measurement error.

For Barcelona's planners, the policy implications are precise. First, target healthcare, schools, and parks in the Low-Low clusters of the northern periphery (Figure \ref{fig:lisa}). Second, think about market access, not just distance to one centre: a neighbourhood's position relative to the entire city matters. Third, do not wait for convergence to close the gap --- eight years of data show the spatial income pattern barely budges. Improving service access in underserved areas requires deliberate investment.

\newpage

# References

\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}

Abbiasov, T., Heine, C., Sabouri, S., Salazar-Miranda, A., Santi, P., Glaeser, E., and Ratti, C. (2024). The 15-Minute City Quantified Using Human Mobility Data. *Nature Human Behaviour*, 8(3), 445--455.

Ajuntament de Barcelona (2021). *Barcelona Superblock*. Urban Mobility Plan. Barcelona City Council.

Allen, T. and Arkolakis, C. (2014). Trade and the Topography of the Spatial Economy. *Quarterly Journal of Economics*, 129(3), 1085--1140.

Anselin, L. (1988). *Spatial Econometrics: Methods and Models*. Kluwer Academic Publishers.

Bivand, R.S., Pebesma, E., and Gomez-Rubio, V. (2013). *Applied Spatial Data Analysis with R*. Springer. Second Edition.

Caliendo, L., Parro, F., Rossi-Hansberg, E., and Sarte, P.-D. (2018). The Impact of Regional and Sectoral Productivity Changes on the US Economy. *Review of Economic Studies*, 85(4), 2042--2096.

Diamond, R. and Suarez Serrato, J.C. (2025). Spatial Sorting and Inequality. NBER Working Paper No.\ 33609.

Donaldson, D. (2018). Railroads of the Raj: Estimating the Impact of Transportation Infrastructure. *American Economic Review*, 108(4--5), 899--934.

Eaton, J. and Kortum, S. (2002). Technology, Geography, and Trade. *Econometrica*, 70(5), 1741--1779.

Ellder, E. (2024). The 15-Minute City Dilemma? Balancing Local Accessibility and Gentrification in Gothenburg, Sweden. *Transportation Research Part D*, 135, 104360.

Ellder, E. (2025). Exploring Socio-Economic Inequalities in Access to the 15-Minute City Across 200 Swedish Built-Up Areas. *Journal of Transport Geography*, 122, 104057.

Ferrer-Ortiz, C., Marquet, O., Mojica, L., and Vich, G. (2022). Barcelona Under the 15-Minute City Lens: Mapping the Accessibility and Proximity Potential Based on Pedestrian Travel Times. *Smart Cities*, 5(1), 146--161.

Harris, C.D. (1954). The Market as a Factor in the Localization of Industry in the United States. *Annals of the Association of American Geographers*, 44(4), 315--348.

LeSage, J. and Pace, R.K. (2009). *Introduction to Spatial Econometrics*. CRC Press.

Moreno, C., Allam, Z., Chabaud, D., Gall, C., and Pratlong, F. (2021). Introducing the "15-Minute City": Sustainability, Resilience and Place Identity in Future Post-Pandemic Cities. *Smart Cities*, 4(1), 93--111.

Mouratidis, K. (2024). Time to Challenge the 15-Minute City: Seven Pitfalls for Sustainability, Equity, Livability, and Spatial Analysis. *Cities*, 153, 105274.

Padgham, M., Lovelace, R., Salmon, M., and Rudis, B. (2019). dodgr: An R Package for Network Flow Aggregation. *Findings*, Transport Findings. doi: 10.32866/6945.

Plaza-Herrera, D. and Mercade-Aloy, J. (2025). Inclusive 15-Minute Cities: An Age-Sensitive Assessment of Active Accessibility in the Metropolitan Area of Barcelona. *Cities*, 165, 106091.

Redding, S.J. (2016). Goods Trade, Factor Mobility and Welfare. *Journal of International Economics*, 101, 148--167.

Redding, S.J. and Rossi-Hansberg, E. (2017). Quantitative Spatial Economics. *Annual Review of Economics*, 9, 21--58.

Thaury, M.-O., Genet, S., Maurice, L., Tubaro, P., and Berkemer, S.J. (2024). City Composition and Accessibility Statistics in and Around Paris. *Frontiers in Big Data*, 7, 1354007.

UBE (2024). 15-Minute Cities: The Urban Planning Model Redefining How We Live. University of the Built Environment, London.
